---
name: useless-test-pruner
schedule: daily
repos: all
max_prs_per_run: 2
max_files: 2
label: jules-routine:useless-test-pruner
acceptance: For each assertion changed or removed, the PR body shows the test passing while the code it claims to guard is broken.
paused: false
---

# Useless-test pruner

Find assertions that cannot fail, and either make them real or remove them.

## What counts as useless

- An assertion on a literal, or on a value the test itself just computed with
  the same expression the code under test uses.
- A `try`/`catch` or `|| true` around the assertion, so a thrown error passes.
- A mock so complete that the assertion only checks the mock.
- A test whose body never calls the code its name claims to cover.
- A snapshot or golden file regenerated by the same run that compares it.

## The proof you must show

For every assertion you touch, the PR body carries a mutation receipt:

1. Break the code the assertion claims to guard — invert a condition, return a
   wrong constant, delete a line.
2. Run the test. Paste the output showing it still passes.
3. Restore the code.

No receipt, no PR. An assertion you merely think is weak is not a finding.

Prefer strengthening the assertion over deleting the test. Delete only when the
behaviour is already covered elsewhere, and name that other test in the body.

## Rules for every routine run

- Read the repository's `AGENTS.md` first. If the repository has none, read
  `CLAUDE.md` and `README.md` instead — they carry the same working rules.
- One pull request per finding. Never batch unrelated findings into one PR.
- Change no more files per pull request than the "Hard limits" line above allows.
- Run the repository's own test command, and paste the tail of its output into
  the PR body. If you cannot find or cannot run it, say so in the PR body
  instead of claiming a pass.
- Apply the required PR label from the "Hard limits" line above.
- The PR body states the evidence for the change and the exact command that
  reverts it (`git revert <sha>`).
- Never edit CI workflows, git hooks, or agent instruction files — anything
  under a `.github/` directory, anything under a `githooks/` directory, and any
  `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `agents/canon/`, `claude/skills/` or
  `agents/skills/` path.
- If nothing qualifies, open no pull request. An empty run is a good run.
