# User identity and platform-specific config are in .gitconfig.local
# (generated per-machine by setup.sh, not committed)
[include]
	path = ~/.gitconfig.local
[safe]
	directory = /home/jckee/dev/dotfiles
	directory = /claude-memory
