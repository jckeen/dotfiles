# Dotfiles

Personal config files. Clone and run `./setup.sh` on a new machine.

## What's included
- `claude/` — Claude Code global settings and instructions
- `.gitconfig` — Git user config and editor preference
